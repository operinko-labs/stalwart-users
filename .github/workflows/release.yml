---
name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: org-runner
    env:
      REGISTRY: ${{ secrets.HARBOR_URL }}
      IMAGE_PREFIX: ${{ secrets.HARBOR_URL }}/operinko-labs
    steps:
      - uses: actions/checkout@v4

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true

      - name: Set image tag
        id: tag
        run: echo "tag=${GITHUB_REF#refs/tags/}" >> "$GITHUB_OUTPUT"

      - name: Set up Buildah/Podman
        run: |
          echo "BUILDAH_ISOLATION=chroot" >> $GITHUB_ENV
          sudo apt-get update
          sudo apt-get -y install buildah podman uidmap libcap2 libcap2-bin
          sudo setcap cap_setuid+eip /usr/bin/newuidmap
          sudo setcap cap_setgid+eip /usr/bin/newgidmap
          sudo chmod u-s /usr/bin/newuidmap
          sudo chmod u-s /usr/bin/newgidmap
          mkdir -p "$HOME/.docker"
          echo -e "[storage]\ndriver = \"vfs\"" | sudo tee /etc/containers/storage.conf > /dev/null

      - name: Log in to Harbor
        uses: redhat-actions/podman-login@4934294ad0449894bcd1e9f191899d7292469603 # v1.7
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.HARBOR_USERNAME }}
          password: ${{ secrets.HARBOR_PASSWORD }}

      - name: Build image
        id: build
        uses: redhat-actions/buildah-build@7a95fa7ee0f02d552a32753e7414641a04307056 # v2.13
        with:
          image: ${{ env.IMAGE_PREFIX }}/stalwart-users
          tags: ${{ steps.tag.outputs.tag }}
          containerfiles: Dockerfile
          context: .
          layers: true

      - name: Push image
        run: podman push ${{ steps.build.outputs.image }}:${{ steps.tag.outputs.tag }}

      - name: Build frontend image
        id: build-frontend
        uses: redhat-actions/buildah-build@7a95fa7ee0f02d552a32753e7414641a04307056 # v2.13
        with:
          image: ${{ env.IMAGE_PREFIX }}/stalwart-users-frontend
          tags: ${{ steps.tag.outputs.tag }}
          containerfiles: Dockerfile.frontend
          context: .
          layers: true

      - name: Push frontend image
        run: podman push ${{ steps.build-frontend.outputs.image }}:${{ steps.tag.outputs.tag }}
